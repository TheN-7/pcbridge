# Copy this file to keystore.properties (same folder) and fill in your
# own values -- keystore.properties itself is gitignored and never
# committed. See README.md's "Signing & CI" section for how to generate
# the actual keystore file this points to.

storeFile=keystore/pcbridge-release.jks
storePassword=
keyAlias=pcbridge
keyPassword=
