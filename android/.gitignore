# Gradle / Android Studio
.gradle/
.idea/
local.properties
build/
app/build/
*.iml
.cxx/
captures/

# Release signing -- NEVER commit. See README.md's "Signing & CI"
# section. Losing the real .jks means every future release needs a new
# signing key and every existing install needs an uninstall+reinstall.
keystore.properties
keystore/

# CI build output (built fresh each run, not something to track)
*.apk
*.aab

# OS cruft
.DS_Store
Thumbs.db
